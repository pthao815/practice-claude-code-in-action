Analyze this project and provide a concise summary covering three areas:

## 1. Architecture
- How the app is structured at a high level (data flow, key abstractions, folder layout)
- How the major pieces connect (contexts, providers, API routes, etc.)

## 2. Tech Stack
- Frameworks, libraries, and tools in use (check package.json, config files, imports)
- Database, auth, styling, testing, and build tooling

## 3. Key Features
- What the app actually does from a user perspective
- Any notable technical features (e.g. streaming, real-time preview, sandboxing)

Keep the summary concise and developer-focused. Use bullet points and short paragraphs. Read only what is necessary — start with CLAUDE.md, package.json, and the src/ directory structure.
